# 🏗️ COMPLETE ARCHITECTURE - HR & HOSPITAL SYSTEM

## 📊 **SYSTEM ARCHITECTURE DIAGRAM**

```
┌────────────────────────────────────────────────────────────────────┐
│                                                                      │
│                          CLIENT LAYER                               │
│                                                                      │
│  ┌──────────────┐    ┌──────────────┐    ┌──────────────┐         │
│  │   Hospital   │    │  HR Admin    │    │   Hotel      │         │
│  │  Web/Mobile  │    │  Interface   │    │   Interface  │         │
│  └──────┬───────┘    └──────┬───────┘    └──────┬───────┘         │
│         │                   │                   │                   │
└────────────────────────────────────────────────────────────────────┘
         │                   │                   │
         │                   │                   │
         ▼                   ▼                   ▼
┌────────────────────────────────────────────────────────────────────┐
│                                                                      │
│                      API GATEWAY LAYER                              │
│                   (Port 6000 - Node.js)                             │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐   │
│  │  Gateway Routes:                                            │   │
│  │  - /api/gateway/health           (Health check)            │   │
│  │  - /api/gateway/systems          (System status)           │   │
│  │  - /api/gateway/hospital/*       (Hospital proxy)          │   │
│  │  - /api/gateway/hr/*             (HR proxy + Basic Auth)   │   │
│  │  - /api/gateway/hotel/*          (Hotel proxy)             │   │
│  │  - /api/gateway/sync/*           (Data sync)               │   │
│  │  - /api/gateway/reports/*        (System reports)          │   │
│  └─────────────────────────────────────────────────────────────┘   │
│                                                                      │
└────────────────────────────────────────────────────────────────────┘
         │                   │                   │
         │                   │                   │
    ┌────▼────┐         ┌────▼────┐         ┌───▼────┐
    │          │         │          │         │        │
    ▼          ▼         ▼          ▼         ▼        ▼
┌──────────┐ ┌──────────────────────────────────────────────┐
│ Hospital │ │  HR Java API  │  Hotel API  │  Email Service │
│ API      │ │  (Port 8080)  │  (TBD)      │  (TBD)         │
│(Port5000)│ └──────────────────────────────────────────────┘
└──────────┘
    │
    ▼
┌──────────────────────────────────────────────────────────────────────┐
│                                                                        │
│                      DATABASE LAYER                                   │
│                                                                        │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐       │
│  │  Hospital DB │      │   HR DB      │      │  Hotel DB    │       │
│  │  MongoDB     │      │  MySQL       │      │  TBD         │       │
│  │              │      │              │      │              │       │
│  │ Collections: │      │ Tables:      │      │ Tables:      │       │
│  │ - doctors    │      │ - employees  │      │ - rooms      │       │
│  │ - patients   │      │ - departments│      │ - bookings   │       │
│  │ - checkups   │      │ - salaries   │      │              │       │
│  │ - appt.      │      │ - attendance │      │              │       │
│  │ - medicines  │      │              │      │              │       │
│  │ - surgery    │      │              │      │              │       │
│  │ - lab        │      │              │      │              │       │
│  │ - contact    │      │              │      │              │       │
│  └──────────────┘      └──────────────┘      └──────────────┘       │
│                                                                        │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 **AUTHENTICATION FLOW**

```
┌─────────────────────────────────────────────────────────────┐
│                   REQUEST FLOW                              │
└─────────────────────────────────────────────────────────────┘

1. CLIENT SENDS REQUEST
   ┌──────────────────────────────────────────┐
   │ POST /api/doctors                        │
   │ {                                        │
   │   "Authorization": "Bearer token_xyz"    │
   │   OR                                     │
   │   "?token=token_xyz"                     │
   │ }                                        │
   └──────────────────────────────────────────┘
                    │
                    ▼
2. API GATEWAY
   ┌──────────────────────────────────────────┐
   │ Receive request on port 6000             │
   │ Route: /api/gateway/hospital/*           │
   └──────────────────────────────────────────┘
                    │
                    ▼
3. AUTHENTICATION MIDDLEWARE
   ┌──────────────────────────────────────────┐
   │ Extract token from:                      │
   │ - Authorization header (Bearer)          │
   │ - Query parameter (?token=)              │
   │ - Cookie                                 │
   │                                          │
   │ Verify JWT token                         │
   │ If valid: req.user = decoded JWT payload │
   │ If invalid: Return 401 Unauthorized      │
   └──────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │ Valid                 │ Invalid
        ▼                       ▼
   Continue              Return error
                         { code: 3 }
                    │
                    ▼
4. VALIDATION MIDDLEWARE
   ┌──────────────────────────────────────────┐
   │ Check request data:                      │
   │ - Required fields present?               │
   │ - Data types correct?                    │
   │ - Data within acceptable ranges?         │
   │                                          │
   │ If valid: Continue                       │
   │ If invalid: Return 400 Bad Request       │
   │            { code: 1 }                   │
   └──────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │ Valid                 │ Invalid
        ▼                       ▼
   Continue              Return error
                    │
                    ▼
5. AUTHORIZATION MIDDLEWARE
   ┌──────────────────────────────────────────┐
   │ Check user role:                         │
   │ req.user.role === 'admin'?               │
   │ req.user.role in ['admin', 'doctor']?    │
   │                                          │
   │ If authorized: Continue                  │
   │ If not: Return 403 Forbidden             │
   │         { code: 4 }                      │
   └──────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │ Authorized            │ Unauthorized
        ▼                       ▼
   Continue              Return error
                    │
                    ▼
6. CONTROLLER/BUSINESS LOGIC
   ┌──────────────────────────────────────────┐
   │ Process request:                         │
   │ - Interact with database                 │
   │ - Apply business rules                   │
   │ - Return result                          │
   └──────────────────────────────────────────┘
                    │
        ┌───────────┴───────────┐
        │ Success               │ Error
        ▼                       ▼
   { code: 0 }         { code: 5 }
                    │
                    ▼
7. LOGGING MIDDLEWARE
   ┌──────────────────────────────────────────┐
   │ Log request and response                 │
   │ Log timestamp, duration, status          │
   └──────────────────────────────────────────┘
                    │
                    ▼
8. ERROR HANDLING MIDDLEWARE (if error)
   ┌──────────────────────────────────────────┐
   │ Catch any errors                         │
   │ Format error response                    │
   │ Send error code (1-5)                    │
   └──────────────────────────────────────────┘
                    │
                    ▼
9. SEND RESPONSE TO CLIENT
   ┌──────────────────────────────────────────┐
   │ Response status + JSON                   │
   │ {                                        │
   │   "code": 0,                             │
   │   "message": "Success",                  │
   │   "success": true,                       │
   │   "data": { ... }                        │
   │ }                                        │
   └──────────────────────────────────────────┘
```

---

## 🔄 **DATA SYNC FLOW**

```
SCENARIO: HR creates new Employee → Should sync to Hospital as Doctor

┌──────────────────────────────────────────────────────────┐
│                   HR SYSTEM (Java)                        │
│                                                           │
│  POST /api/employees                                     │
│  {                                                       │
│    "emp_id": 101,                                        │
│    "emp_name": "Dr. Nguyen Van A",                       │
│    "position": "Doctor",                                 │
│    "department": "Cardiology",                           │
│    "salary": 50000                                       │
│  }                                                       │
│                                                           │
│  ✅ Employee created in HR DB                           │
└──────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│              SYNC TRIGGER (Every 5 minutes)              │
│                                                           │
│  GET /api/gateway/sync/employees                         │
│  (API Gateway polls HR for new employees)               │
└──────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                API GATEWAY (Node.js)                      │
│                                                           │
│  1. Fetch all employees from HR API                      │
│  2. Map HR fields → Hospital fields                      │
│                                                           │
│     HR Employee:                                         │
│     {                                                    │
│       "emp_id": 101,                                    │
│       "emp_name": "Dr. Nguyen Van A",                   │
│       "department": "Cardiology"                         │
│     }                                                    │
│     ↓                                                    │
│     ↓ (Data Mapper)                                     │
│     ↓                                                    │
│     Hospital Doctor:                                    │
│     {                                                    │
│       "name": "Dr. Nguyen Van A",                       │
│       "specialization": "Cardiologist",                 │
│       "department": "Cardiology",                        │
│       "hr_employee_id": 101,                            │
│       "sync_status": "synced",                          │
│       "sync_date": "2024-02-04"                         │
│     }                                                    │
│                                                           │
│  3. Send to Hospital API                                │
└──────────────────────────────────────────────────────────┘
                         │
                         ▼
┌──────────────────────────────────────────────────────────┐
│                 HOSPITAL SYSTEM                           │
│                                                           │
│  POST /api/doctors (internal sync request)              │
│  {                                                       │
│    "name": "Dr. Nguyen Van A",                          │
│    "specialization": "Cardiologist",                    │
│    "department": "Cardiology",                          │
│    "hr_employee_id": 101,                               │
│    "sync_status": "synced"                              │
│  }                                                       │
│                                                           │
│  ✅ Doctor created/updated in Hospital DB              │
└──────────────────────────────────────────────────────────┘
```

---

## 📊 **ERROR CODE MAPPING**

```
┌─────────────────────────────────────────────────────┐
│                   ERROR CODES                        │
├──────────┬──────────────────┬───────────────────────┤
│ CODE     │ MEANING          │ HTTP STATUS          │
├──────────┼──────────────────┼───────────────────────┤
│ 0        │ ✅ Success       │ 200/201               │
├──────────┼──────────────────┼───────────────────────┤
│ 1        │ ❌ Missing data  │ 400 Bad Request       │
│          │    Invalid data  │ (Validation error)    │
├──────────┼──────────────────┼───────────────────────┤
│ 2        │ ❌ Duplicate     │ 409 Conflict          │
│          │    Record exists │ (Already in database) │
├──────────┼──────────────────┼───────────────────────┤
│ 3        │ ❌ Invalid token │ 401 Unauthorized      │
│          │    No auth       │ (Not authenticated)   │
├──────────┼──────────────────┼───────────────────────┤
│ 4        │ ❌ No permission │ 403 Forbidden         │
│          │    Wrong role    │ (Not authorized)      │
├──────────┼──────────────────┼───────────────────────┤
│ 5        │ ❌ Server error  │ 500 Server Error      │
│          │    DB error      │ (Database/Server)     │
└──────────┴──────────────────┴───────────────────────┘

RESPONSE FORMAT:
{
  "code": 0-5,
  "message": "Human readable message",
  "success": true/false,
  "data": { ... }       // If success
  "error": "..."        // If error
  "timestamp": "ISO"
}
```

---

## 🎯 **USE CASE TO CODE MAPPING**

### **Use Case: Create Doctor**

```
USE CASE:                          CODE IMPLEMENTATION:
─────────────────────────────────────────────────────────

1. Admin sends POST request    →   Route: POST /api/doctors
   with doctor data

2. Check authentication        →   isAuthenticated middleware
   - Has valid token?          →   Verify JWT token

3. Validate request data       →   validateDoctor middleware
   - All required fields?       →   Check name, specialization, dept
   - Correct data types?        →   Ensure string/number types

4. Check authorization         →   roleBasedAccess(['admin'])
   - Is user admin?             →   Check req.user.role === 'admin'

5. Process data                →   createDoctor controller
   - Check for duplicates       →   Query existing doctors
   - Create in database         →   Doctor.create(data)
   - Return created doctor      →   Return doctor object

6. Handle errors               →   errorHandler middleware
   - Database error?            →   Return code: 5
   - Duplicate found?           →   Return code: 2
   - Validation failed?         →   Return code: 1

7. Log request                 →   logging middleware
   - Method, path, status       →   Console.log
   - Duration                   →   Measure time
```

---

## 🏥 **HOSPITAL SYSTEM - COMPLETE FLOW**

```
┌─────────────────────────────────────────────────────────┐
│  1. Hospital Admin logs in                              │
│     POST /api/auth/login                                │
│     Request: { username: "admin", password: "..." }     │
│     Response: { token: "jwt_xyz", role: "admin" }       │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  2. Admin creates doctor                                │
│     POST /api/doctors?token=jwt_xyz                      │
│     Request: {                                          │
│       "name": "Dr. A",                                  │
│       "specialization": "Cardiology",                   │
│       "department": "Heart"                             │
│     }                                                   │
│                                                          │
│     Middleware flow:                                    │
│     ├─ isAuthenticated ✓                               │
│     ├─ validateDoctor ✓                                │
│     ├─ roleBasedAccess(['admin']) ✓                    │
│     └─ createDoctor (saves to DB)                      │
│                                                          │
│     Response: { code: 0, data: { id, name, ... } }     │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  3. Patient views doctors (public endpoint)             │
│     GET /api/doctors                                    │
│     (No token needed)                                   │
│                                                          │
│     Response: { code: 0, data: [doctors...] }           │
└─────────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────┐
│  4. Patient books appointment                           │
│     POST /api/appointments?token=jwt_patient            │
│     Request: {                                          │
│       "doctor_id": "...",                               │
│       "date": "2024-02-15",                             │
│       "time": "10:00"                                   │
│     }                                                   │
│                                                          │
│     Response: { code: 0, data: { appointment } }        │
└─────────────────────────────────────────────────────────┘
```

---

## 🔗 **GATEWAY INTEGRATION FLOW**

```
┌──────────────────────────────────────────────────────┐
│  Hospital Client                                      │
│  GET /api/gateway/hospital/doctors                   │
└──────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│  API GATEWAY (Port 6000)                             │
│                                                      │
│  Route: /api/gateway/hospital/*                      │
│  ├─ Forward to Hospital API on port 5000             │
│  ├─ Hospital auth handling                           │
│  └─ Return response to client                        │
└──────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│  Hospital API (Port 5000)                            │
│  GET /api/doctors                                    │
│                                                      │
│  Response: { doctors array }                         │
└──────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│  API GATEWAY (Port 6000)                             │
│  Format response                                     │
│  Return to client                                    │
└──────────────────────────────────────────────────────┘
                    │
                    ▼
┌──────────────────────────────────────────────────────┐
│  Hospital Client receives                            │
│  { code: 0, data: { doctors } }                      │
└──────────────────────────────────────────────────────┘
```

---

## ✅ **IMPLEMENTATION CHECKLIST**

### **Phase 1: Core Middleware**
- [ ] isAuthenticated.js - Token validation
- [ ] roleBasedAccess.js - Permission checking
- [ ] validateDoctor.js - Data validation
- [ ] errorHandler.js - Error handling
- [ ] logging.js - Request logging

### **Phase 2: Routes & Controllers**
- [ ] doctorRoutes.js - All doctor endpoints
- [ ] appointmentRoutes.js - Appointment management
- [ ] demoRoutes.js - Public demo endpoints
- [ ] Controllers for each route

### **Phase 3: Integration**
- [ ] API Gateway (gateway.mjs)
- [ ] HR API integration
- [ ] Data sync mechanism
- [ ] Hotel integration (TBD)

### **Phase 4: Security & Testing**
- [ ] Test all middleware
- [ ] Test error codes
- [ ] Test authentication
- [ ] Test authorization
- [ ] Test data validation

---

**Architecture is the foundation of a scalable system!** 🏗️
